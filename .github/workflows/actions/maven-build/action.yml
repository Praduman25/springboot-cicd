name: "Maven Build Action"

description: "Setup Java and build Spring Boot application"

runs:
  using: "composite"

  steps:

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build with Maven
      run: mvn clean install
      shell: bash